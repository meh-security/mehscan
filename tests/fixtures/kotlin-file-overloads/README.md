# Kotlin File overload controls

Original Spring scalar-input fixture for charset-bearing text reads/writes and
append operations. Request-selected paths are separate from request-controlled
content. The fixed write and fixed allowlisted read are negative path controls.
The private direct-call harness uses a temporary directory and its own sibling
files; it does not access deployed application files or sensitive resources.

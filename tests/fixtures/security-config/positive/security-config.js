function review(serialized) {
  tls.connect({ rejectUnauthorized: false });
  response.cookie("transport", "value", { secure: false });
  response.cookie("script", "value", { httpOnly: false });
  crypto.createHash("md5");
  yaml.load(serialized);
}

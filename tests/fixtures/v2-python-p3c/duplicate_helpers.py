import subprocess


def launch(command):
    return subprocess.Popen(command, shell=True)


def launch(command):
    return subprocess.Popen(command, shell=True)


def caller(value):
    return launch(value)

# Kotlin Servlet cookie controls

Original isolated Jakarta Servlet cookie helpers cover sensitive session flags,
later overrides, distinct cookie instances, nonsensitive preferences and unused
objects. Qualified Java setters and Kotlin synthetic properties have the same
SDK effect. The response is an owned harness dependency; no deployed servlet
container, browser, proxy rewrite or production transport is established.

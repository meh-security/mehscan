# Kotlin authorization inventory

Canonical Spring annotations, aliases, qualified names and vararg role policies
are possible guard evidence. This fixture does not activate Spring, instantiate a
proxy, expose endpoints or prove a method is intercepted. A foreign same-named
annotation is excluded. Absence or presence of an annotation alone is not an
application authorization verdict.

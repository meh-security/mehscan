- id: csharp-filesystem-read
  version: 2
  title: .NET filesystem read
  language: csharp
  kind: sink
  capability: filesystem_read
  cwe: [CWE-22]
  match:
    any:
      - pattern: File.ReadAllText($PATH)
        captures: { path: PATH }
      - pattern: File.ReadAllBytes($PATH)
        captures: { path: PATH }
      - pattern: File.OpenRead($PATH)
        captures: { path: PATH }
      - pattern: File.ReadAllLines($PATH)
        captures: { path: PATH }
      - pattern: File.ReadLines($PATH)
        captures: { path: PATH }
      - pattern: File.OpenText($PATH)
        captures: { path: PATH }
      - pattern: Directory.GetFiles($PATH)
        captures: { path: PATH }
      - pattern: Directory.GetFiles($PATH, $$$ARGUMENTS)
        captures: { path: PATH }
      - pattern: Directory.EnumerateFiles($PATH)
        captures: { path: PATH }
      - pattern: Directory.EnumerateFiles($PATH, $$$ARGUMENTS)
        captures: { path: PATH }
      - pattern: Directory.GetDirectories($PATH)
        captures: { path: PATH }
      - pattern: Directory.GetDirectories($PATH, $$$ARGUMENTS)
        captures: { path: PATH }
      - pattern: File.Copy($PATH, $DESTINATION)
        captures: { path: PATH, destination: DESTINATION }
      - pattern: File.Copy($PATH, $DESTINATION, $OVERWRITE)
        captures: { path: PATH, destination: DESTINATION }
  symbols:
    - canonical: System.IO.File.ReadAllText
      captures: { path: 0 }
    - canonical: System.IO.File.ReadAllBytes
      captures: { path: 0 }
    - canonical: System.IO.File.OpenRead
      captures: { path: 0 }
    - canonical: System.IO.File.ReadAllLines
      captures: { path: 0 }
    - canonical: System.IO.File.ReadLines
      captures: { path: 0 }
    - canonical: System.IO.File.OpenText
      captures: { path: 0 }
    - canonical: System.IO.Directory.GetFiles
      captures: { path: 0 }
    - canonical: System.IO.Directory.EnumerateFiles
      captures: { path: 0 }
    - canonical: System.IO.Directory.GetDirectories
      captures: { path: 0 }
    - canonical: System.IO.File.Copy
      captures: { path: 0, destination: 1 }
  tags: [filesystem, path]
  ai: &filesystem_guidance
    investigate:
      - origin and normalization of the path
      - allowed roots, traversal segments, symlinks, and platform path behavior
      - authorization and tenant boundaries for the referenced resource
  provenance: &csharp_filesystem_provenance
    note: .NET filesystem APIs mapped to the CWE-22 investigation surface.
    references:
      - https://cwe.mitre.org/data/definitions/22.html
      - https://learn.microsoft.com/en-us/dotnet/api/system.io.file

- id: csharp-filesystem-write
  version: 2
  title: .NET filesystem write
  language: csharp
  kind: sink
  capability: filesystem_write
  cwe: [CWE-22]
  match:
    any:
      - pattern: File.WriteAllText($PATH, $CONTENT)
        captures: { path: PATH }
      - pattern: File.WriteAllBytes($PATH, $CONTENT)
        captures: { path: PATH }
      - pattern: File.OpenWrite($PATH)
        captures: { path: PATH }
      - pattern: File.Create($PATH)
        captures: { path: PATH }
      - pattern: File.AppendAllText($PATH, $CONTENT)
        captures: { path: PATH }
      - pattern: File.AppendAllLines($PATH, $CONTENT)
        captures: { path: PATH }
      - pattern: File.Delete($PATH)
        captures: { path: PATH }
      - pattern: Directory.CreateDirectory($PATH)
        captures: { path: PATH }
      - pattern: Directory.Delete($PATH)
        captures: { path: PATH }
      - pattern: Directory.Delete($PATH, $RECURSIVE)
        captures: { path: PATH }
      - pattern: Directory.Move($SOURCE, $PATH)
        captures: { source: SOURCE, path: PATH }
  symbols:
    - canonical: System.IO.File.WriteAllText
      captures: { path: 0 }
    - canonical: System.IO.File.WriteAllBytes
      captures: { path: 0 }
    - canonical: System.IO.File.OpenWrite
      captures: { path: 0 }
    - canonical: System.IO.File.Create
      captures: { path: 0 }
    - canonical: System.IO.File.AppendAllText
      captures: { path: 0 }
    - canonical: System.IO.File.AppendAllLines
      captures: { path: 0 }
    - canonical: System.IO.File.Delete
      captures: { path: 0 }
    - canonical: System.IO.Directory.CreateDirectory
      captures: { path: 0 }
    - canonical: System.IO.Directory.Delete
      captures: { path: 0 }
    - canonical: System.IO.Directory.Move
      captures: { source: 0, path: 1 }
  tags: [filesystem, path]
  ai: *filesystem_guidance
  provenance: *csharp_filesystem_provenance

- id: java-filesystem-read
  version: 1
  title: Java filesystem read
  language: java
  kind: sink
  capability: filesystem_read
  cwe: [CWE-22]
  match:
    any:
      - pattern: Files.readString($PATH)
        captures: { path: PATH }
      - pattern: Files.readAllBytes($PATH)
        captures: { path: PATH }
      - pattern: Files.newBufferedReader($PATH)
        captures: { path: PATH }
      - pattern: Files.newInputStream($PATH, $$$ARGUMENTS)
        captures: { path: PATH }
      - pattern: Files.copy($PATH, $DESTINATION, $$$ARGUMENTS)
        captures: { path: PATH, destination: DESTINATION }
      - pattern: Files.copy($PATH, $DESTINATION)
        captures: { path: PATH, destination: DESTINATION }
  symbols:
    - canonical: java.nio.file.Files.readString
      captures: { path: 0 }
    - canonical: java.nio.file.Files.readAllBytes
      captures: { path: 0 }
    - canonical: java.nio.file.Files.newBufferedReader
      captures: { path: 0 }
    - canonical: java.nio.file.Files.newInputStream
      captures: { path: 0 }
    - canonical: java.nio.file.Files.copy
      captures: { path: 0, destination: 1 }
  tags: [filesystem, path]
  ai: *filesystem_guidance
  provenance: &java_filesystem_provenance
    note: Java NIO filesystem APIs mapped to the CWE-22 investigation surface.
    references:
      - https://cwe.mitre.org/data/definitions/22.html
      - https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/nio/file/Files.html

- id: java-filesystem-write
  version: 1
  title: Java filesystem write
  language: java
  kind: sink
  capability: filesystem_write
  cwe: [CWE-22]
  match:
    any:
      - pattern: Files.write($PATH, $CONTENT)
        captures: { path: PATH }
      - pattern: Files.write($PATH, $CONTENT, $$$ARGUMENTS)
        captures: { path: PATH }
      - pattern: Files.writeString($PATH, $CONTENT)
        captures: { path: PATH }
      - pattern: Files.writeString($PATH, $CONTENT, $$$ARGUMENTS)
        captures: { path: PATH }
      - pattern: Files.newBufferedWriter($PATH, $$$ARGUMENTS)
        captures: { path: PATH }
      - pattern: Files.copy($SOURCE, $PATH, $$$ARGUMENTS)
        captures: { source: SOURCE, path: PATH }
      - pattern: Files.copy($SOURCE, $PATH)
        captures: { source: SOURCE, path: PATH }
      - pattern: Files.move($SOURCE, $PATH, $$$ARGUMENTS)
        captures: { source: SOURCE, path: PATH }
      - pattern: Files.move($SOURCE, $PATH)
        captures: { source: SOURCE, path: PATH }
      - pattern: Files.delete($PATH)
        captures: { path: PATH }
      - pattern: Files.deleteIfExists($PATH)
        captures: { path: PATH }
      - pattern: Files.createDirectory($PATH, $$$ARGUMENTS)
        captures: { path: PATH }
      - pattern: Files.createDirectories($PATH, $$$ARGUMENTS)
        captures: { path: PATH }
  symbols:
    - canonical: java.nio.file.Files.write
      captures: { path: 0 }
    - canonical: java.nio.file.Files.writeString
      captures: { path: 0 }
    - canonical: java.nio.file.Files.newBufferedWriter
      captures: { path: 0 }
    - canonical: java.nio.file.Files.copy
      captures: { source: 0, path: 1 }
    - canonical: java.nio.file.Files.move
      captures: { source: 0, path: 1 }
    - canonical: java.nio.file.Files.delete
      captures: { path: 0 }
    - canonical: java.nio.file.Files.deleteIfExists
      captures: { path: 0 }
    - canonical: java.nio.file.Files.createDirectory
      captures: { path: 0 }
    - canonical: java.nio.file.Files.createDirectories
      captures: { path: 0 }
  tags: [filesystem, path]
  ai: *filesystem_guidance
  provenance: *java_filesystem_provenance

- id: javascript-filesystem-read
  version: 1
  title: Node.js filesystem read
  language: javascript
  kind: sink
  capability: filesystem_read
  cwe: [CWE-22]
  match: &node_filesystem_read_match
    any:
      - pattern: fs.readFile($PATH, $$$ARGUMENTS)
        captures: { path: PATH }
      - pattern: fs.readFileSync($PATH)
        captures: { path: PATH }
      - pattern: fs.readFileSync($PATH, $$$ARGUMENTS)
        captures: { path: PATH }
      - pattern: fs.createReadStream($PATH)
        captures: { path: PATH }
      - pattern: fs.createReadStream($PATH, $$$ARGUMENTS)
        captures: { path: PATH }
      - pattern: fs.promises.readFile($PATH)
        captures: { path: PATH }
      - pattern: fs.promises.readFile($PATH, $$$ARGUMENTS)
        captures: { path: PATH }
      - pattern: fs.copyFile($PATH, $DESTINATION, $$$ARGUMENTS)
        captures: { path: PATH, destination: DESTINATION }
      - pattern: fs.copyFileSync($PATH, $DESTINATION)
        captures: { path: PATH, destination: DESTINATION }
      - pattern: fs.copyFileSync($PATH, $DESTINATION, $$$ARGUMENTS)
        captures: { path: PATH, destination: DESTINATION }
      - pattern: fs.promises.copyFile($PATH, $DESTINATION)
        captures: { path: PATH, destination: DESTINATION }
      - pattern: fs.promises.copyFile($PATH, $DESTINATION, $$$ARGUMENTS)
        captures: { path: PATH, destination: DESTINATION }
  symbols: &node_filesystem_read_symbols
    - canonical: fs.readFile
      captures: { path: 0 }
    - canonical: fs.readFileSync
      captures: { path: 0 }
    - canonical: fs.createReadStream
      captures: { path: 0 }
    - canonical: fs.promises.readFile
      captures: { path: 0 }
    - canonical: fs.copyFile
      captures: { path: 0, destination: 1 }
    - canonical: fs.copyFileSync
      captures: { path: 0, destination: 1 }
    - canonical: fs.promises.copyFile
      captures: { path: 0, destination: 1 }
  tags: [filesystem, path, nodejs]
  ai: *filesystem_guidance
  provenance: &node_filesystem_provenance
    note: Node.js filesystem APIs mapped to the CWE-22 investigation surface.
    references:
      - https://cwe.mitre.org/data/definitions/22.html
      - https://nodejs.org/api/fs.html

- id: javascript-filesystem-write
  version: 1
  title: Node.js filesystem write
  language: javascript
  kind: sink
  capability: filesystem_write
  cwe: [CWE-22]
  match: &node_filesystem_write_match
    any:
      - pattern: fs.writeFile($PATH, $CONTENT, $$$ARGUMENTS)
        captures: { path: PATH }
      - pattern: fs.writeFileSync($PATH, $CONTENT)
        captures: { path: PATH }
      - pattern: fs.writeFileSync($PATH, $CONTENT, $$$ARGUMENTS)
        captures: { path: PATH }
      - pattern: fs.createWriteStream($PATH)
        captures: { path: PATH }
      - pattern: fs.createWriteStream($PATH, $$$ARGUMENTS)
        captures: { path: PATH }
      - pattern: fs.promises.writeFile($PATH, $CONTENT, $$$ARGUMENTS)
        captures: { path: PATH }
      - pattern: fs.rename($SOURCE, $PATH, $$$ARGUMENTS)
        captures: { source: SOURCE, path: PATH }
      - pattern: fs.renameSync($SOURCE, $PATH)
        captures: { source: SOURCE, path: PATH }
      - pattern: fs.copyFile($SOURCE, $PATH, $$$ARGUMENTS)
        captures: { source: SOURCE, path: PATH }
      - pattern: fs.copyFileSync($SOURCE, $PATH, $$$ARGUMENTS)
        captures: { source: SOURCE, path: PATH }
      - pattern: fs.copyFileSync($SOURCE, $PATH)
        captures: { source: SOURCE, path: PATH }
      - pattern: fs.unlink($PATH, $$$ARGUMENTS)
        captures: { path: PATH }
      - pattern: fs.unlinkSync($PATH)
        captures: { path: PATH }
      - pattern: fs.rm($PATH, $$$ARGUMENTS)
        captures: { path: PATH }
      - pattern: fs.rmSync($PATH, $$$ARGUMENTS)
        captures: { path: PATH }
      - pattern: fs.rmSync($PATH)
        captures: { path: PATH }
      - pattern: fs.mkdir($PATH, $$$ARGUMENTS)
        captures: { path: PATH }
      - pattern: fs.mkdirSync($PATH, $$$ARGUMENTS)
        captures: { path: PATH }
      - pattern: fs.mkdirSync($PATH)
        captures: { path: PATH }
      - pattern: fs.promises.rename($SOURCE, $PATH)
        captures: { source: SOURCE, path: PATH }
      - pattern: fs.promises.copyFile($SOURCE, $PATH)
        captures: { source: SOURCE, path: PATH }
      - pattern: fs.promises.copyFile($SOURCE, $PATH, $$$ARGUMENTS)
        captures: { source: SOURCE, path: PATH }
      - pattern: fs.promises.unlink($PATH)
        captures: { path: PATH }
      - pattern: fs.promises.rm($PATH)
        captures: { path: PATH }
      - pattern: fs.promises.rm($PATH, $$$ARGUMENTS)
        captures: { path: PATH }
      - pattern: fs.promises.mkdir($PATH)
        captures: { path: PATH }
      - pattern: fs.promises.mkdir($PATH, $$$ARGUMENTS)
        captures: { path: PATH }
  symbols: &node_filesystem_write_symbols
    - canonical: fs.writeFile
      captures: { path: 0 }
    - canonical: fs.writeFileSync
      captures: { path: 0 }
    - canonical: fs.createWriteStream
      captures: { path: 0 }
    - canonical: fs.promises.writeFile
      captures: { path: 0 }
    - canonical: fs/promises.writeFile
      captures: { path: 0 }
    - canonical: fs.rename
      captures: { source: 0, path: 1 }
    - canonical: fs.renameSync
      captures: { source: 0, path: 1 }
    - canonical: fs.copyFile
      captures: { source: 0, path: 1 }
    - canonical: fs.copyFileSync
      captures: { source: 0, path: 1 }
    - canonical: fs.unlink
      captures: { path: 0 }
    - canonical: fs.unlinkSync
      captures: { path: 0 }
    - canonical: fs.rm
      captures: { path: 0 }
    - canonical: fs.rmSync
      captures: { path: 0 }
    - canonical: fs.mkdir
      captures: { path: 0 }
    - canonical: fs.mkdirSync
      captures: { path: 0 }
    - canonical: fs.promises.rename
      captures: { source: 0, path: 1 }
    - canonical: fs.promises.copyFile
      captures: { source: 0, path: 1 }
    - canonical: fs.promises.unlink
      captures: { path: 0 }
    - canonical: fs.promises.rm
      captures: { path: 0 }
    - canonical: fs.promises.mkdir
      captures: { path: 0 }
  tags: [filesystem, path, nodejs]
  ai: *filesystem_guidance
  provenance: *node_filesystem_provenance

- id: typescript-filesystem-read
  version: 1
  title: Node.js filesystem read
  language: typescript
  kind: sink
  capability: filesystem_read
  cwe: [CWE-22]
  match: *node_filesystem_read_match
  symbols: *node_filesystem_read_symbols
  tags: [filesystem, path, nodejs]
  ai: *filesystem_guidance
  provenance: *node_filesystem_provenance

- id: typescript-filesystem-write
  version: 1
  title: Node.js filesystem write
  language: typescript
  kind: sink
  capability: filesystem_write
  cwe: [CWE-22]
  match: *node_filesystem_write_match
  symbols: *node_filesystem_write_symbols
  tags: [filesystem, path, nodejs]
  ai: *filesystem_guidance
  provenance: *node_filesystem_provenance

- id: tsx-filesystem-read
  version: 1
  title: Node.js filesystem read
  language: tsx
  kind: sink
  capability: filesystem_read
  cwe: [CWE-22]
  match: *node_filesystem_read_match
  symbols: *node_filesystem_read_symbols
  tags: [filesystem, path, nodejs]
  ai: *filesystem_guidance
  provenance: *node_filesystem_provenance

- id: tsx-filesystem-write
  version: 1
  title: Node.js filesystem write
  language: tsx
  kind: sink
  capability: filesystem_write
  cwe: [CWE-22]
  match: *node_filesystem_write_match
  symbols: *node_filesystem_write_symbols
  tags: [filesystem, path, nodejs]
  ai: *filesystem_guidance
  provenance: *node_filesystem_provenance

- id: python-filesystem-read
  version: 1
  title: Python filesystem read
  language: python
  kind: sink
  capability: filesystem_read
  cwe: [CWE-22]
  match:
    any:
      - pattern: open($PATH)
        captures: { path: PATH }
      - pattern: open($PATH, "r")
        captures: { path: PATH }
      - pattern: open($PATH, "rb")
        captures: { path: PATH }
      - pattern: open($PATH, 'r')
        captures: { path: PATH }
      - pattern: open($PATH, 'rb')
        captures: { path: PATH }
      - pattern: codecs.open($PATH, $$$ARGUMENTS)
        captures: { path: PATH }
      - pattern: pathlib.Path($PATH).read_text()
        captures: { path: PATH }
      - pattern: pathlib.Path($PATH).read_text($$$ARGUMENTS)
        captures: { path: PATH }
      - pattern: pathlib.Path($PATH).read_bytes()
        captures: { path: PATH }
      - pattern: shutil.copy($PATH, $DESTINATION)
        captures: { path: PATH, destination: DESTINATION }
      - pattern: shutil.copy2($PATH, $DESTINATION)
        captures: { path: PATH, destination: DESTINATION }
  tags: [filesystem, path]
  ai: *filesystem_guidance
  provenance: &python_filesystem_provenance
    note: Python file-opening APIs mapped to the CWE-22 investigation surface.
    references:
      - https://cwe.mitre.org/data/definitions/22.html
      - https://docs.python.org/3/library/functions.html#open

- id: python-filesystem-write
  version: 1
  title: Python filesystem write
  language: python
  kind: sink
  capability: filesystem_write
  cwe: [CWE-22]
  match:
    any:
      - pattern: open($PATH, "w")
        captures: { path: PATH }
      - pattern: open($PATH, "wb")
        captures: { path: PATH }
      - pattern: open($PATH, "a")
        captures: { path: PATH }
      - pattern: open($PATH, 'w')
        captures: { path: PATH }
      - pattern: open($PATH, 'wb')
        captures: { path: PATH }
      - pattern: open($PATH, 'a')
        captures: { path: PATH }
      - pattern: os.remove($PATH)
        captures: { path: PATH }
      - pattern: os.unlink($PATH)
        captures: { path: PATH }
      - pattern: shutil.copy($SOURCE, $PATH)
        captures: { source: SOURCE, path: PATH }
      - pattern: shutil.copy2($SOURCE, $PATH)
        captures: { source: SOURCE, path: PATH }
      - pattern: shutil.move($SOURCE, $PATH)
        captures: { source: SOURCE, path: PATH }
      - pattern: pathlib.Path($PATH).write_text($CONTENT)
        captures: { path: PATH }
      - pattern: pathlib.Path($PATH).write_bytes($CONTENT)
        captures: { path: PATH }
      - pattern: pathlib.Path($PATH).unlink()
        captures: { path: PATH }
  symbols:
    - canonical: os.remove
      captures: { path: 0 }
    - canonical: os.unlink
      captures: { path: 0 }
    - canonical: shutil.copy
      captures: { source: 0, path: 1 }
    - canonical: shutil.copy2
      captures: { source: 0, path: 1 }
    - canonical: shutil.move
      captures: { source: 0, path: 1 }
  tags: [filesystem, path]
  ai: *filesystem_guidance
  provenance: *python_filesystem_provenance

- id: go-filesystem-read
  version: 1
  title: Go filesystem read
  language: go
  kind: sink
  capability: filesystem_read
  cwe: [CWE-22]
  match:
    any:
      - pattern: os.Open($PATH)
        context: |
          package pattern
          func patternContext() { os.Open($PATH) }
        selector: call_expression
        captures: { path: PATH }
      - pattern: os.ReadFile($PATH)
        context: |
          package pattern
          func patternContext() { os.ReadFile($PATH) }
        selector: call_expression
        captures: { path: PATH }
      - pattern: os.OpenFile($PATH, $FLAGS, $PERMISSIONS)
        captures: { path: PATH }
  symbols:
    - canonical: os.Open
      captures: { path: 0 }
    - canonical: os.ReadFile
      captures: { path: 0 }
    - canonical: os.OpenFile
      captures: { path: 0 }
  tags: [filesystem, path]
  ai: *filesystem_guidance
  provenance: &go_filesystem_provenance
    note: Go os package APIs mapped to the CWE-22 investigation surface.
    references:
      - https://cwe.mitre.org/data/definitions/22.html
      - https://pkg.go.dev/os

- id: go-filesystem-write
  version: 1
  title: Go filesystem write
  language: go
  kind: sink
  capability: filesystem_write
  cwe: [CWE-22]
  match:
    any:
      - pattern: os.Create($PATH)
        context: |
          package pattern
          func patternContext() { os.Create($PATH) }
        selector: call_expression
        captures: { path: PATH }
      - pattern: os.WriteFile($PATH, $CONTENT, $PERMISSIONS)
        captures: { path: PATH }
      - pattern: os.Remove($PATH)
        context: |
          package pattern
          func patternContext() { os.Remove($PATH) }
        selector: call_expression
        captures: { path: PATH }
      - pattern: os.Rename($SOURCE, $PATH)
        captures: { source: SOURCE, path: PATH }
      - pattern: os.RemoveAll($PATH)
        captures: { path: PATH }
      - pattern: os.Mkdir($PATH, $PERMISSIONS)
        captures: { path: PATH }
      - pattern: os.MkdirAll($PATH, $PERMISSIONS)
        captures: { path: PATH }
  symbols:
    - canonical: os.Create
      captures: { path: 0 }
    - canonical: os.WriteFile
      captures: { path: 0 }
    - canonical: os.Remove
      captures: { path: 0 }
    - canonical: os.Rename
      captures: { source: 0, path: 1 }
    - canonical: os.RemoveAll
      captures: { path: 0 }
    - canonical: os.Mkdir
      captures: { path: 0 }
    - canonical: os.MkdirAll
      captures: { path: 0 }
  tags: [filesystem, path]
  ai: *filesystem_guidance
  provenance: *go_filesystem_provenance

- id: rust-filesystem-read
  version: 1
  title: Rust filesystem read
  language: rust
  kind: sink
  capability: filesystem_read
  cwe: [CWE-22]
  match:
    any:
      - pattern: std::fs::read($PATH)
        captures: { path: PATH }
      - pattern: std::fs::read_to_string($PATH)
        captures: { path: PATH }
      - pattern: std::fs::read_dir($PATH)
        captures: { path: PATH }
      - pattern: File::open($PATH)
        captures: { path: PATH }
      - pattern: std::fs::File::open($PATH)
        captures: { path: PATH }
      - pattern: std::fs::copy($PATH, $DESTINATION)
        captures: { path: PATH, destination: DESTINATION }
  tags: [filesystem, path, rust]
  ai: *filesystem_guidance
  provenance: &rust_filesystem_provenance
    note: Rust standard filesystem entrypoints mapped to path-traversal review; imported short names require local identity verification.
    references:
      - https://cwe.mitre.org/data/definitions/22.html
      - https://doc.rust-lang.org/std/fs/

- id: rust-filesystem-write
  version: 1
  title: Rust filesystem write or removal
  language: rust
  kind: sink
  capability: filesystem_write
  cwe: [CWE-22]
  match:
    any:
      - pattern: std::fs::write($PATH, $CONTENT)
        captures: { path: PATH, content: CONTENT }
      - pattern: std::fs::remove_file($PATH)
        captures: { path: PATH }
      - pattern: std::fs::remove_dir_all($PATH)
        captures: { path: PATH }
      - pattern: File::create($PATH)
        captures: { path: PATH }
      - pattern: std::fs::File::create($PATH)
        captures: { path: PATH }
      - pattern: std::fs::copy($SOURCE, $PATH)
        captures: { source: SOURCE, path: PATH }
      - pattern: std::fs::rename($SOURCE, $PATH)
        captures: { source: SOURCE, path: PATH }
      - pattern: std::fs::create_dir($PATH)
        captures: { path: PATH }
      - pattern: std::fs::create_dir_all($PATH)
        captures: { path: PATH }
      - pattern: std::fs::remove_dir($PATH)
        captures: { path: PATH }
  tags: [filesystem, path, rust]
  ai: *filesystem_guidance
  provenance: *rust_filesystem_provenance

- id: c-filesystem-read
  version: 1
  title: C filesystem read
  language: c
  kind: sink
  capability: filesystem_read
  cwe: [CWE-22]
  match: &native_filesystem_read
    any:
      - pattern: fopen($PATH, "r")
        context: |
          void pattern_context() { fopen($PATH, "r"); }
        selector: call_expression
        captures: { path: PATH }
      - pattern: fopen($PATH, "rb")
        context: |
          void pattern_context() { fopen($PATH, "rb"); }
        selector: call_expression
        captures: { path: PATH }
      - pattern: open($PATH, O_RDONLY)
        context: |
          void pattern_context() { open($PATH, O_RDONLY); }
        selector: call_expression
        captures: { path: PATH }
  tags: [filesystem, path, native]
  ai: &native_filesystem_guidance
    investigate:
      - origin, normalization, and encoding of the path
      - allowed roots, traversal segments, symlinks, races, and platform path behavior
      - authorization and privilege boundaries for the referenced resource
    verify:
      - resolve the effective open mode and the path value reaching the operating system
    exclude:
      - do not treat a fixed path or an API observation alone as path traversal
  provenance: &native_filesystem_provenance
    note: Exact native open modes and path-taking filesystem APIs are mapped to the shared filesystem evidence surface.
    references:
      - https://cwe.mitre.org/data/definitions/22.html
      - https://pubs.opengroup.org/onlinepubs/9799919799/functions/open.html

- id: c-filesystem-write
  version: 1
  title: C filesystem write or removal
  language: c
  kind: sink
  capability: filesystem_write
  cwe: [CWE-22]
  match: &native_filesystem_write
    any:
      - pattern: fopen($PATH, "w")
        context: |
          void pattern_context() { fopen($PATH, "w"); }
        selector: call_expression
        captures: { path: PATH }
      - pattern: fopen($PATH, "wb")
        context: |
          void pattern_context() { fopen($PATH, "wb"); }
        selector: call_expression
        captures: { path: PATH }
      - pattern: fopen($PATH, "a")
        context: |
          void pattern_context() { fopen($PATH, "a"); }
        selector: call_expression
        captures: { path: PATH }
      - pattern: open($PATH, O_WRONLY)
        context: |
          void pattern_context() { open($PATH, O_WRONLY); }
        selector: call_expression
        captures: { path: PATH }
      - pattern: open($PATH, O_RDWR)
        context: |
          void pattern_context() { open($PATH, O_RDWR); }
        selector: call_expression
        captures: { path: PATH }
      - pattern: remove($PATH)
        context: |
          void pattern_context() { remove($PATH); }
        selector: call_expression
        captures: { path: PATH }
      - pattern: unlink($PATH)
        context: |
          void pattern_context() { unlink($PATH); }
        selector: call_expression
        captures: { path: PATH }
      - pattern: rename($SOURCE, $PATH)
        context: |
          void pattern_context() { rename($SOURCE, $PATH); }
        selector: call_expression
        captures: { source: SOURCE, path: PATH }
  tags: [filesystem, path, native]
  ai: *native_filesystem_guidance
  provenance: *native_filesystem_provenance

- id: cpp-filesystem-read
  version: 1
  title: C++ filesystem read
  language: cpp
  kind: sink
  capability: filesystem_read
  cwe: [CWE-22]
  match: *native_filesystem_read
  tags: [filesystem, path, native, cpp]
  ai: *native_filesystem_guidance
  provenance: *native_filesystem_provenance

- id: cpp-filesystem-write
  version: 1
  title: C++ filesystem write or removal
  language: cpp
  kind: sink
  capability: filesystem_write
  cwe: [CWE-22]
  match: *native_filesystem_write
  tags: [filesystem, path, native, cpp]
  ai: *native_filesystem_guidance
  provenance: *native_filesystem_provenance

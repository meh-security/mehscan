- id: kotlin-runtime-exec
  version: 1
  title: Kotlin JVM runtime command execution
  language: kotlin
  kind: sink
  capability: process_execution
  cwe: [CWE-78]
  match:
    any:
      - pattern: java.lang.Runtime.getRuntime().exec($COMMAND)
        captures: { command: COMMAND }
      - pattern: $RUNTIME.getRuntime().exec($COMMAND)
        captures: { command: COMMAND }
  tags: [kotlin, jvm, command]
  ai: &guidance
    investigate:
      - Verify API identity, input origin, execution context and applicable controls.
      - This boundary is a review lead and does not establish attacker control.
      - At canonical Spring MVC mappings, a non-simple unannotated handler parameter normally uses model-attribute request binding. Verify the supplied model property and applicable binder policy; binding=false and another argument resolver can disable that binding.
    verify:
      - Confirm the API resolves to the JVM library and the call executes in production.
      - For digests, verify the selected algorithm and whether the use needs cryptographic strength.
    exclude:
      - Exclude local package lookalikes and inert examples.
      - Exclude strong digest choices and non-security checksums from weak-cryptography findings.
  provenance:
    note: JVM Runtime identity resolved from fully qualified names, imports, aliases or default imports with lexical shadow guards.
    references:
      - https://cwe.mitre.org/data/definitions/78.html
      - https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/lang/Runtime.html

- id: kotlin-files-read
  version: 1
  title: Kotlin JVM filesystem read
  language: kotlin
  kind: sink
  capability: filesystem_read
  cwe: [CWE-22]
  match:
    any:
      - pattern: java.nio.file.Files.readAllBytes($PATH)
        captures: { path: PATH }
      - pattern: java.nio.file.Files.readString($PATH)
        captures: { path: PATH }
      - pattern: $FILES.readAllBytes($PATH)
        captures: { path: PATH }
      - pattern: $FILES.readString($PATH)
        captures: { path: PATH }
  tags: [kotlin, jvm, filesystem]
  ai: *guidance
  provenance: &filesystem_provenance
    note: JVM Files identity resolved from fully qualified names, imports or aliases with lexical shadow guards.
    references:
      - https://cwe.mitre.org/data/definitions/22.html
      - https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/nio/file/Files.html

- id: kotlin-files-write
  version: 1
  title: Kotlin JVM filesystem write
  language: kotlin
  kind: sink
  capability: filesystem_write
  cwe: [CWE-22]
  match:
    any:
      - pattern: java.nio.file.Files.write($PATH, $CONTENT)
        captures: { path: PATH, content: CONTENT }
      - pattern: java.nio.file.Files.writeString($PATH, $CONTENT)
        captures: { path: PATH, content: CONTENT }
      - pattern: $FILES.write($PATH, $CONTENT)
        captures: { path: PATH, content: CONTENT }
      - pattern: $FILES.writeString($PATH, $CONTENT)
        captures: { path: PATH, content: CONTENT }
  tags: [kotlin, jvm, filesystem]
  ai:
    investigate:
      - Verify canonical Files identity, execution context and the path capture's exact value origin.
      - This CWE-22 candidate concerns selection of the target path. Track path and content arguments separately; request content does not establish attacker-selected path syntax.
    verify:
      - Establish whether the caller can escape the intended target directory through the path argument, accounting for containment and symlink policy.
    exclude:
      - Writing request content to a fixed server-owned path alone is ordinary file-output behavior, not path traversal. Do not infer an authorization or integrity policy violation without supplied evidence of that policy.
  provenance: *filesystem_provenance

- id: kotlin-message-digest
  version: 1
  title: Kotlin JVM digest algorithm selection
  language: kotlin
  kind: security_configuration
  capability: cryptographic_hash
  cwe: [CWE-327]
  match:
    any:
      - pattern: java.security.MessageDigest.getInstance($ALGORITHM)
        captures: { algorithm: ALGORITHM }
      - pattern: $DIGEST.getInstance($ALGORITHM)
        captures: { algorithm: ALGORITHM }
  tags: [kotlin, jvm, crypto]
  ai: *guidance
  provenance:
    note: Explicit JVM digest selection in Kotlin; algorithm inventory only.
    references:
      - https://cwe.mitre.org/data/definitions/327.html
      - https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/security/MessageDigest.html

- id: kotlin-uri-parsing
  version: 1
  title: Kotlin JVM URI parsing
  language: kotlin
  kind: validation
  capability: url_parsing
  cwe: [CWE-918]
  match:
    any:
      - pattern: java.net.URI.create($URL)
        captures: { url: URL }
      - pattern: $URI.create($URL)
        captures: { url: URL }
  tags: [kotlin, jvm, url]
  ai: *guidance
  provenance:
    note: Explicit JVM URI parsing in Kotlin; parsing does not validate destination policy.
    references:
      - https://cwe.mitre.org/data/definitions/918.html
      - https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/net/URI.html

- id: kotlin-persistence-query
  version: 1
  title: Kotlin persistence query execution
  language: kotlin
  kind: sink
  capability: database_query
  cwe: [CWE-89]
  match:
    any:
      - pattern: $MANAGER.createQuery($QUERY)
        captures: { query: QUERY }
      - pattern: $MANAGER.createQuery($QUERY, $TYPE)
        captures: { query: QUERY }
      - pattern: $MANAGER.createNativeQuery($QUERY)
        captures: { query: QUERY }
      - pattern: $MANAGER.createNativeQuery($QUERY, $TYPE)
        captures: { query: QUERY }
  tags: [kotlin, jvm, sql, persistence]
  ai: *guidance
  provenance:
    note: Exact declared javax/jakarta EntityManager receiver; query boundary alone does not establish attacker control or unsafe construction.
    references:
      - https://cwe.mitre.org/data/definitions/89.html
      - https://jakarta.ee/specifications/persistence/3.2/apidocs/jakarta.persistence/jakarta/persistence/entitymanager

- id: kotlin-jdbc-statement-query
  version: 1
  title: Kotlin JDBC statement SQL execution
  language: kotlin
  kind: sink
  capability: database_query
  cwe: [CWE-89]
  match:
    any:
      - pattern: $STATEMENT.executeQuery($QUERY)
        captures: { query: QUERY }
      - pattern: $STATEMENT.execute($QUERY)
        captures: { query: QUERY }
      - pattern: $STATEMENT.executeUpdate($QUERY)
        captures: { query: QUERY }
      - pattern: $STATEMENT.addBatch($QUERY)
        captures: { query: QUERY }
  tags: [kotlin, jvm, sql, jdbc]
  ai: *guidance
  provenance:
    note: Canonical declared or bounded immutable-local factory java.sql.Statement receiver; caller-supplied SQL is distinct from separately bound PreparedStatement values.
    references:
      - https://cwe.mitre.org/data/definitions/89.html
      - https://docs.oracle.com/en/java/javase/25/docs/api/java.sql/java/sql/Statement.html

- id: kotlin-jdbc-prepare-query
  version: 1
  title: Kotlin JDBC prepared SQL construction
  language: kotlin
  kind: sink
  capability: database_query
  cwe: [CWE-89]
  match:
    any:
      - pattern: $CONNECTION.prepareStatement($QUERY)
        captures: { query: QUERY }
      - pattern: $CONNECTION.prepareStatement($QUERY, $OPTION)
        captures: { query: QUERY }
      - pattern: $CONNECTION.prepareCall($QUERY)
        captures: { query: QUERY }
  tags: [kotlin, jvm, sql, jdbc, preparation]
  ai: *guidance
  provenance:
    note: Canonical declared or bounded immutable-local factory java.sql.Connection receiver; preparation does not sanitize SQL already constructed from request text. Establish subsequent execution before claiming runtime query impact.
    references:
      - https://cwe.mitre.org/data/definitions/89.html
      - https://docs.oracle.com/en/java/javase/25/docs/api/java.sql/java/sql/Connection.html

- id: kotlin-jdbc-template-query
  version: 1
  title: Kotlin Spring JdbcTemplate SQL execution
  language: kotlin
  kind: sink
  capability: database_query
  cwe: [CWE-89]
  match:
    any:
      - pattern: $TEMPLATE.queryForList($QUERY)
        captures: { query: QUERY }
      - pattern: $TEMPLATE.queryForList($QUERY, $ARGUMENT)
        captures: { query: QUERY }
      - pattern: $TEMPLATE.queryForList($QUERY, $ARGUMENT, $OPTIONS)
        captures: { query: QUERY }
      - pattern: $TEMPLATE.queryForObject($QUERY, $ARGUMENT)
        captures: { query: QUERY }
      - pattern: $TEMPLATE.queryForObject($QUERY, $ARGUMENT, $OPTIONS)
        captures: { query: QUERY }
      - pattern: $TEMPLATE.query($QUERY, $ARGUMENT)
        captures: { query: QUERY }
      - pattern: $TEMPLATE.query($QUERY, $ARGUMENT, $OPTIONS)
        captures: { query: QUERY }
      - pattern: $TEMPLATE.update($QUERY)
        captures: { query: QUERY }
      - pattern: $TEMPLATE.update($QUERY, $ARGUMENT)
        captures: { query: QUERY }
      - pattern: $TEMPLATE.execute($QUERY)
        captures: { query: QUERY }
  tags: [kotlin, spring, sql, jdbc]
  ai: *guidance
  provenance:
    note: Exact declared org.springframework.jdbc.core.JdbcTemplate receiver; captures the SQL argument, not separately bound values. Explicitly typed non-String callback operands and direct lambdas are excluded; compiler overload resolution remains partial.
    references:
      - https://cwe.mitre.org/data/definitions/89.html
      - https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/jdbc/core/JdbcTemplate.html

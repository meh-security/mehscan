# Third-party notices

Mehscan includes selected source components from ast-grep 0.45.1:

- `ast-grep-core`
- `ast-grep-config`
- `ast-grep-language`
- `ast-grep-outline`

Upstream: <https://github.com/ast-grep/ast-grep>

These components are distributed under the MIT License. The preserved license
and source attribution are in [`crates/ast-grep/LICENSE`](crates/ast-grep/LICENSE)
and [`crates/ast-grep/UPSTREAM.md`](crates/ast-grep/UPSTREAM.md).

Rust package dependencies retain their respective licenses. Exact dependency
versions are recorded in `Cargo.lock`.
